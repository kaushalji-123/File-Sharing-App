import {io} from "socket.io-client";

const SocketProvider = () =>{
    const [socket, setSocket] = useState(second)

    useEffect(() => {
      first
    
      return () => {
        second
      }
    }, [third])
    
}